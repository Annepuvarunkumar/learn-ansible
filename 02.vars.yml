- name: Variables
  hosts: localhost
  connection: local
  vars:
    URL: play.google.com
  tasks:
    - name: Print Variables
      ansible.builtin.debug:
        msg: URL = {{ URL }}

    - name: Print task Variables
      ansible.builtin.debug:
        msg: URL = {{ URL }}
      vars:
        URL: task.google.com
